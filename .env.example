# Browser Control MCP Server Environment Variables

# Extension Secret - Required for secure communication with Firefox extension
# Generate this secret from the Firefox extension's options page
EXTENSION_SECRET=your_secret_here

# Screenshot Directory - Optional directory path for saving screenshot files
# If not set, defaults to './screenshots' relative to the server working directory
# Can be either a relative or absolute path
# Example Windows path: D:\\screenshots
# Example Unix path: /home/user/screenshots
# The server will create this directory if it doesn't exist
SCREENSHOT_DIR=./screenshots